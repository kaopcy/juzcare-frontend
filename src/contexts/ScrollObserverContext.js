import { createContext } from 'react'

const initialValue = {
   scrollY: null,
}

const ScrollObserverContext = createContext(initialValue)

export const ScrollObserverProvider = ({ children })=>{

}


